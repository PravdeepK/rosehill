/** Canonical marketing site — match Search / social (www). */
export const SITE_URL = "https://www.rosehilldesignbuild.com";

export const SITE_NAME = "Rose Hill Design Build";

/**
 * Shown after each page name in `<title>`, Open Graph, and Twitter cards —
 * e.g. `Careers | Leaders in Luxury`. Add `title: "Careers"` (or similar) in
 * each route's `metadata` export; the root layout supplies this suffix.
 */
export const PAGE_TITLE_TAGLINE = "Leaders in Luxury";

/** Default & Open Graph / Twitter description (brand name first). */
export const SITE_DESCRIPTION =
  "Rose Hill Design Build delivers high-end design-build services specializing in luxury retail, commercial, and residential projects across North America.";
